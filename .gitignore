# 1. Bỏ qua các thư viện nặng
node_modules/
backend/node_modules/
frontend/node_modules/

# 2. Bỏ qua các file môi trường (Cực kỳ quan trọng để bảo mật)
.env
.env.local
.env.development
.env.production
backend/.env
frontend/.env

# 3. Bỏ qua các thư mục build của Frontend
frontend/dist/
frontend/build/

# 4. Bỏ qua file log và hệ thống
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# 5. Bỏ qua database local của Docker (nếu có)
mongo_data/